% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_functions.R
\name{optimal_weights}
\alias{optimal_weights}
\title{Compute Optimal Portfolio Weights}
\usage{
optimal_weights(
  W,
  factors_list,
  loadings_list,
  residual_covariance,
  T_total,
  p
)
}
\arguments{
\item{W}{An integer specifying the window size for portfolio optimization.}

\item{factors_list}{A list of factor matrices for each time period.}

\item{loadings_list}{A list of loading matrices corresponding to each factor matrix.}

\item{residual_covariance}{A covariance matrix of the residuals.}

\item{T_total}{An integer representing the total number of time periods.}

\item{p}{An integer representing the number of assets.}
}
\value{
A list of optimal portfolio weights for each time period beyond the window.
}
\description{
This function computes the optimal portfolio weights based on time-varying covariance matrices
using quadratic programming.
}
\details{
The function iterates over each time period beyond the initial window \code{W} and computes
the optimal portfolio weights that minimize variance using the inverse of the time-varying
covariance matrix. It utilizes the \code{quadprog} package for quadratic programming.
}
\examples{
# Example parameters
W <- 60
factors_list <- list(matrix(rnorm(300), ncol = 5))
loadings_list <- list(matrix(runif(25), ncol = 5))
residual_covariance <- diag(0.01, 5)
T_total <- 200
p <- 5

# Compute optimal weights
optimal_weights <- optimal_weights(W, factors_list, loadings_list, residual_covariance, T_total, p)
print(optimal_weights[[61]])

}
