% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis_testing.R
\name{compute_J_pT}
\alias{compute_J_pT}
\title{Compute \eqn{J_{pT}} Statistic for Covariance Time-Variation Hypothesis Testing}
\usage{
compute_J_pT(B_pT, V_pT, M_hat, T, p, h)
}
\arguments{
\item{B_pT}{A numeric scalar representing the \eqn{B_{pT}} statistic.}

\item{V_pT}{A numeric scalar representing the \eqn{V_{pT}} statistic.}

\item{M_hat}{A numeric scalar representing the \eqn{M_{\hat{}}} statistic.}

\item{T}{An integer specifying the number of time periods.}

\item{p}{An integer specifying the number of assets.}

\item{h}{A numeric value indicating the bandwidth parameter used in kernel functions.}
}
\value{
A numeric scalar \eqn{J_{pT}} representing the computed test statistic.
}
\description{
This function calculates the \eqn{J_{pT}} statistic, which is used to test the null
hypothesis that the covariance matrix of asset returns is not time-varying. The statistic
compares the scaled \eqn{M_{\hat{}}} and \eqn{B_{pT}} against the square root of \eqn{V_{pT}}.
}
\details{
The \eqn{J_{pT}} statistic is computed using the formula:
\deqn{
J_{pT} = \frac{T \sqrt{p} \sqrt{h} M_{\hat{}} - B_{pT}}{\sqrt{V_{pT}}}
}

This statistic is used to assess whether there is significant evidence to reject the null
hypothesis of time-invariant covariance matrices. Typically, if \eqn{J_{pT}} exceeds
the critical value (e.g., 1.96 for a 5\% significance level), the null hypothesis is rejected.
}
\examples{
# Example values
B_pT <- 0.5
V_pT <- 0.1
M_hat <- 0.3
T <- 100
p <- 50
h <- 0.1

# Compute J_pT
J_pT <- compute_J_pT(B_pT, V_pT, M_hat, T, p, h)
print(J_pT)

}
